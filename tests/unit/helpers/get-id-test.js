import { getId } from 'ecommerce/helpers/get-id';
import { module, test } from 'qunit';

module('Unit | Helper | get id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getId([42]);
  assert.ok(result);
});
