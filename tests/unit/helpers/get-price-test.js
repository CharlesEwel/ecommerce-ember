import { getPrice } from 'ecommerce/helpers/get-price';
import { module, test } from 'qunit';

module('Unit | Helper | get price');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getPrice([42]);
  assert.ok(result);
});
