import { getQuantity } from 'ecommerce/helpers/get-quantity';
import { module, test } from 'qunit';

module('Unit | Helper | get quantity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getQuantity([42]);
  assert.ok(result);
});
